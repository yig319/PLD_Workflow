# Fresh pip-based install for this repository.
# Run from repository root:
#   python -m pip install -r requirements-pip.txt

-e .[visualization,build]
xrayutilities

# Optional (not required for current AFM 2D visualizer path):
# mayavi
